import type { ExpressiveCodeConfig } from "../types/config";

export const expressiveCodeConfig: ExpressiveCodeConfig = {
  // Catatan: Beberapa gaya (seperti warna latar belakang) telah diganti, silakan merujuk ke file astro.config.mjs.
  // Silakan pilih tema gelap, karena tema blog ini saat ini hanya mendukung latar belakang gelap.
  theme: "github-dark",
};